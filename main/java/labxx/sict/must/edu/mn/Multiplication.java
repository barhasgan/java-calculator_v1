package labxx.sict.must.edu.mn;

/**
 * Multiply two double values (conflict version B).
 */
public class Multiplication {

    /**
     * Multiply two numbers (conflict version A).
     */
    public double multiply(double a, double b) {
        return a * b;
    }
}
